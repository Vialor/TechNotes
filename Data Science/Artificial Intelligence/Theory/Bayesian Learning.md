Bayesian Learning: a style of learning that uses Bayes' theorem to determine the conditional probability of a hypotheses given some evidence or observations

Naive Bayes Classifier: features are independent of each other given labels
smoothing: avoid 0

**MAP Maximum A Posteriori**
$\theta_{MAP} = \arg\max_{\theta} P(\theta | D) = \arg\max_{\theta} \frac{P(D | \theta) \cdot P(\theta)}{P(D)}$

**MLE Maximum Likelihood Estimation**
$\theta_{MLE} = \arg\max_{\theta} P(D | \theta)$

??
均匀先验分布（Uniform Prior Distribution of Parameters）
标签均匀分布（Uniform Distribution of Labels）

#TODO